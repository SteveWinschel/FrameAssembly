[package]
name = "frameassembly"
version = "0.4.0"
edition = "2024"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
pnet = "0.34.0"

[profile.release]
codegen_units = 1
lto = "fat"
panic = "abort"
